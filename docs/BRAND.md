# ForgeDock Brand Handles

Status of reserved and blocked brand handles across platforms.

## Domain

| Domain | Status |
|--------|--------|
| `forgedock.dev` | **Not owned** — owner-confirmed 2026-07-05 as NOT controlled by RapierCraft Studios; live content is an unrelated third-party product. Retired from ForgeDock branding — do not use anywhere. |
| `forgedock.io` | **Owned** — owner-confirmed 2026-07-05 as registered to RapierCraft Studios. Canonical ForgeDock domain. Currently a Porkbun parked page; a site/holding page is pending deploy — do not link publicly until the site is live. |

> **Rule**: No public ForgeDock surface (npm homepage, repo description, plugin manifests, docs) may point at a domain until its live content is verified as controlled by RapierCraft Studios.

## npm

| Package | Status |
|---------|--------|
| `forgedock` | **Owned** — published AGPL CLI at https://www.npmjs.com/package/forgedock |

No action needed. The package name was claimed on first publish.

## GitHub Organization

| Slug | Status |
|------|--------|
| `forgedock` | **Blocked** — parked org created 2024-05-23, not administered by RapierCraft Studios |

The bare `forgedock` org slug cannot be used for the ForgeDock GitHub App name — GitHub
slugifies App display names and rejects collisions with existing user/org accounts.

**Canonical public App**: **App ID 4051319** (Client ID `Iv23ligZAftI2QDt5CUa`), owner-confirmed
2026-07-05. Currently owned by the personal `@RapierCraft` account — **to be transferred to the
`RapierCraftStudios` org** (a public/commercial App should not live on a personal account). Its
public listing still needs a real description and a homepage that resolves publicly.

Superseded / legacy Apps — do **not** present as the public identity:
- **3978349** — earlier dev App (registered under RapierCraftStudios, `public: false`); superseded by 4051319.
- **3731547** (`rapiercraft-forge`) — placeholder description "test"; authored the early public merged PRs but is legacy, its listing being wound down.

Recommended slug when finalizing the org-owned App: `forgedock-ai` (the bare `forgedock` slug is blocked, per above).

## Social Handles

| Platform | Handle | Status |
|----------|--------|--------|
| X / Twitter | `@forgedock` | Check and reserve if available |
| Bluesky | `forgedock.bsky.social` | Check and reserve if available |
| LinkedIn | `forgedock` company page | Check and reserve if available |
| Product Hunt | `forgedock` | Reserve at launch |

Social handle registration is an operational task. Reserve handles before public launch
(L1 milestone) to prevent squatting. The public App launch (L2) is gated on L1 completion.

## Summary

| Platform | Action |
|----------|--------|
| Domain (forgedock.dev) | Not owned (owner-confirmed 2026-07-05) — retired from branding |
| Domain (forgedock.io) | Owned — canonical; site deploy pending before public linking |
| npm | Done |
| GitHub org slug | Blocked — use variant for App slug |
| Social handles | Reserve before L1 public launch |
